package command;

import edu.kis.powp.jobs2d.Job2dDriver;

public class CircleFactory {

    public static ComplexCommand createCircle(Job2dDriver driver, int x, int y, int size) {
        ComplexCommand command = new ComplexCommand();

        return command;
    }

}
